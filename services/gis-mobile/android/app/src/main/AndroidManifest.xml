<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Sauvegarde Android : Preferences (CapacitorStorage.xml) est EXCLU. Il contient les
         jetons de session, le consentement de localisation et les files hors ligne du
         chauffeur. Restauré après une réinstallation ou sur un nouveau téléphone, il faisait
         apparaître l'invite système de localisation SANS l'écran d'explication exigé par
         Google Play, reprenait un suivi et rouvrait une session avec un jeton copié
         (relecture du 21/09/2026, constat 29). dataExtractionRules : Android 12+ (sauvegarde
         en ligne ET transfert d'appareil à appareil) ; fullBackupContent : Android 11 et avant. -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: calypso://vehicle/<id>?lat=..&lng=.. — encodé dans le
                 QR du popup monitoring web; ouvre l'appli zoomée sur le véhicule. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="calypso" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Suivi d'une tournée par le téléphone du chauffeur (plugin
         @capacitor-community/background-geolocation) : service au premier plan de
         type « location » avec la notification permanente « Tournée en cours ».
         Android 14+ (API 34) exige cette permission pour démarrer un tel service ;
         le service lui-même (foregroundServiceType="location") est déclaré par le
         manifeste du plugin. PAS de ACCESS_BACKGROUND_LOCATION : le suivi démarre
         toujours depuis l'écran (« Je pars ») et vit dans le service au premier plan. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Le manifeste du plugin background-geolocation déclare la puce GPS sans
         android:required, donc OBLIGATOIRE : Google Play cesserait de proposer
         l'application (et ses mises à jour) aux appareils sans GPS — tablettes Wi-Fi des
         gestionnaires, dont le mode n'a aucun besoin de GPS (constat 30). Sur un tel
         appareil, le suivi d'un chauffeur se contente de ce que donne la position réseau. -->
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false"
        tools:replace="android:required" />

</manifest>
