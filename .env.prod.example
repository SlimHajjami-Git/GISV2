# Production Environment Variables
# Copy this file to .env.prod and fill in your secure values

# PostgreSQL
POSTGRES_PASSWORD=ChangeThisToASecurePassword123!

# RabbitMQ
RABBITMQ_USER=gisadmin
RABBITMQ_PASSWORD=ChangeThisToASecurePassword456!

# JWT Secret (minimum 32 characters)
JWT_SECRET_KEY=YourSuperSecretKeyForProductionAtLeast32CharactersLong!

# Groq AI Chat API Key (get from https://console.groq.com/keys)
GROQ_API_KEY=gsk_your-groq-api-key-here

# Optional: Domain name for SSL
DOMAIN=yourdomain.com

# SMTP / Email — propre a CHAQUE serveur (TN: adresse tunisienne, DZ: adresse algerienne).
# Chaque serveur a son .env.prod, donc ces valeurs different par deploiement.
SMTP_HOST=mailing.topnet.tn
SMTP_PORT=25
SMTP_SSL=0
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=contact@belive.tn
SMTP_FROM_NAME=GPA Belive
